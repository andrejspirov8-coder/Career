# Career Workspace — Single Scorer Logic Audit\n\n**Date:** 20 May 2026 | **Scope:** `recruiter_match.py` + `matching_lib.py` variant scoring\n\n---\n\n## Executive Summary\n\nYour **Single Scorer** (the recruiter profile → CV variant decision engine) is well-architected but reveals three **firmographic weighting blind spots**:\n\n1. **Staffing agency recruiters are underweighted relative to internal hiring managers** — A recruiter at Michael Page gets identical sector scoring as a district manager at a luxury store chain\n2. **Lithuanian market signals are treated as tie-breakers, not rank shifters** — `LT_MARKERS` boost only applies post-matching; a recruiter explicitly looking for \"regiono vadovas\" doesn't rank differently upfront\n3. **Executive search consultants score lower than generalist HR** — No persona weighting for **retained search** (high-authority hiring signals)\n\n---\n\n## Architecture: Three-Layer Matcher\n\n```\nLinkedIn Profile Scrape (headline + company + about + location + role_text)\n        ↓\n   LAYER 1: Gate Terms (hiring_gate_ok) ← Is this person in HR/recruitment/hiring ecosystem?\n        ↓\n   LAYER 2: Sector Keywords (sector_scores) ← Do they mention luxury/retail/operations/IT?\n        ↓\n   LAYER 3: CV Variant Matching (match_job_to_variants) ← How well does their profile match your 4 CV versions?\n        ↓\nFinal Decision: (max(sector_score × 2.2 + cv_primary × 0.4, cv_primary))\n```\n\n### Current Behavior\n\nFrom `recruiter_match.py` line 239–280:\n\n```python\nif sector_slug and sector_top >= sector_beats_cv_min_score:  # sector_beats_cv_min_score = 6.0\n    chosen = sector_slug\n    if chosen == \"luxury-retail\" and LT_MARKERS.search(blob):\n        chosen = \"luxury-retail-lt\"\nelse:\n    chosen = cv_slug\n\nunified = max(sector_top * 2.2 + 0.4 * cv_primary, cv_primary)\n```\n\n**This means:**\n- A sector score of 9.0 becomes a unified score of 9.0 × 2.2 + 0.4 × 12 = **24.8** ✅ (strong)\n- A sector score of 6.0 becomes a unified score of 6.0 × 2.2 + 0.4 × 8 = **16.4** (threshold)\n- If sector < 6.0, CV score alone matters (breaks tie-breaker promise)\n\n---\n\n## Audit: Firmographic Blindspots\n\n### Issue 1: Staffing Agency Recruiters Underweighted\n\n**Current:** Sector keywords for `luxury-retail` include only **job titles**, not **company types**:\n\n```yaml\nsector_keywords:\n  luxury-retail:\n    - \"store director\"\n    - \"retail director\"\n    - \"area manager\"\n    - \"regional manager\"\n    # Missing:\n    # - \"michael page\"  ← Michael Page is internal HR focus, not generic staffing\n    # - \"heidrick & struggles\"\n    # - \"korn ferry\"\n    # - \"executive recruitment\"\n```\n\n**Result:** \n- **Jane Doe @ Michael Kors (Internal HR)**: headline=\"Senior Recruiter, Premium Retail\" → sector_score = **9.0** (\"recruiter\" doesn't exist, but \"premium retail\" hits 3×)\n- **John Smith @ Michael Page**: headline=\"Retail Recruitment Consultant\" → sector_score = **0** (no hits)\n\nBoth should score **high**, but Michael Page gets penalized for **not matching luxury sector keywords** even though:\n1. Michael Page specializes in **retail recruitment** globally\n2. Their team actively recruits for **Michael Kors Baltics**\n3. The tier_1 config already mentions them: `company_signals_any: [\"michael page\"]`\n\n**Recommendation:** Add **company-type boosts** to `recruiter_matching.sector_keywords`:\n\n```yaml\nrecruiter_matching:\n  sector_keywords:\n    luxury-retail:\n      # Job titles (existing)\n      - \"store director\"\n      - \"area manager\"\n      # NEW: Company/agency specialization signals\n      - \"executive recruitment\"\n      - \"executive search\"\n      - \"retained search\"\n      - \"recruitment specialist\"\n      - \"recruitment consultant\"\n      - \"talent acquisition\"\n      - \"headhunter\"\n      # NEW: Tier-1 priority companies\n      - \"michael page\"  # (moved from tier_1 config → sector signal)\n      - \"michael kors\"\n      - \"experis\"\n```\n\n**Impact:** Michael Page staffing recruiter would go from sector_score=0 → sector_score=6+ (now qualifies for tier_1 if other signals align).\n\n---\n\n### Issue 2: Lithuanian Market Signals Arrive Too Late\n\n**Current:** Language preference is **post-decision**:\n\n```python\nif chosen == \"luxury-retail\" and LT_MARKERS.search(blob):\n    chosen = \"luxury-retail-lt\"  # Demote from luxury-retail → luxury-retail-lt\n```\n\n**Problem:** A Lithuanian-based recruiter looking for \"regiono vadovas prabangos prekyba\" has their profile analyzed as **English-first**, even though:\n1. The variant `luxury-retail-lt` has a **dedicated CV** (in Lithuanian)\n2. The tier/config explicitly targets Lithuanian market\n3. You have Lithuanian connection note templates\n\n**Example:**\n- Profile: \"Personalo vadovė @ Apranga Group, Vilnius | Ieškau: regiono vadovo prabangos prekyba\"\n- Current flow: Matched to `luxury-retail` (English CV), then downgraded to `luxury-retail-lt` (Lithuanian CV)\n- **Better flow:** Detect LT upfront, bias scoring toward `luxury-retail-lt`, then use English `luxury-retail` as runner-up\n\n**Recommendation:** Pre-flight language detection:\n\n```python\ndef detect_profile_language(blob: str) -> str:\n    \"\"\"Heuristic: count LT stopwords vs EN stopwords.\"\"\"\n    lt_count = sum(1 for word in LT_STOPWORDS if f\" {word} \" in f\" {blob.lower()} \")\n    en_count = sum(1 for word in EN_STOPWORDS if f\" {word} \" in f\" {blob.lower()} \")\n    return \"lt\" if lt_count > en_count else \"en\"\n\n# Then, in match_recruiter_profile():\nprofile_lang = detect_profile_language(blob_lower)\nif profile_lang == \"lt\":\n    # Reorder variant scoring: luxury-retail-lt first, luxury-retail second\n    scores_ordered = [scores[\"luxury-retail-lt\"], scores[\"luxury-retail\"]] + ...\n```\n\n---\n\n### Issue 3: Executive Search Consultants Score Identically to Generalist HR\n\n**Current:** `GATE_TERMS` treats all hiring ecosystem roles equally:\n\n```python\nDEFAULT_GATE_TERMS = frozenset({\n    \"recruiter\",\n    \"talent acquisition\",\n    \"talent partner\",\n    \"headhunter\",                  # ← Executive search (high authority)\n    \"executive search\",             # ← Executive search (high authority)\n    \"staffing\",                     # ← Low-touch generalist\n    \"personalo\",                    # ← Ukrainian/Russian, no weighting\n    \"hr manager\",                   # ← Generalist HR\n    \"hiring manager\",               # ← Hiring authority\n    # ... 40+ more terms, all weighted equally\n})\n```\n\n**Problem:** All gate terms are binary (pass/fail). No **hierarchical authority weighting**:\n- An **internal Michael Kors HR director** (high authority, direct hiring power) scores = 1 gate_ok\n- A **retained search consultant** (high authority, gatekeeping access to executives) scores = 1 gate_ok\n- A **generic recruiter at Random Staffing** (low authority, high volume) scores = 1 gate_ok\n\n**Impact:** When building your tier system, you can't prioritize by **hiring authority**, only by **CV fit + company signals**.\n\n**Recommendation:** Introduce **persona scoring** (similar to your `hiring_network` config):\n\n```python\nPERSONA_AUTHORITY_WEIGHTS = {\n    \"executive_search\": 95,        # Retained search, C-level gatekeeper\n    \"internal_hr_leader\": 90,      # VP People, Head of HR, Chief People Officer\n    \"hiring_manager\": 85,          # Area Manager, Store Director with hiring power\n    \"in_house_recruiter\": 80,      # Corporate recruiter, internal talent team\n    \"staffing_agency_recruiter\": 70,  # Michael Page, Experis, generalist recruiter\n    \"generic_hr\": 60,              # HR Generalist, HR Administrator\n}\n\ndef detect_recruiter_persona(blob_lower: str) -> tuple[str, int]:\n    \"\"\"Return (persona_slug, authority_weight).\"\"\"\n    if \"executive search\" in blob_lower or \"retained search\" in blob_lower:\n        return (\"executive_search\", 95)\n    if any(t in blob_lower for t in [\"vp people\", \"head of hr\", \"chief people\"]):\n        return (\"internal_hr_leader\", 90)\n    if \"hiring manager\" in blob_lower or (\"manager\" in blob_lower and \"area\" in blob_lower):\n        return (\"hiring_manager\", 85)\n    if \"in-house\" in blob_lower or \"in house\" in blob_lower:\n        return (\"in_house_recruiter\", 80)\n    if any(t in blob_lower for t in [\"michael page\", \"experis\", \"korn ferry\"]):\n        return (\"staffing_agency_recruiter\", 70)\n    if \"recruiter\" in blob_lower or \"talent acquisition\" in blob_lower:\n        return (\"staffing_agency_recruiter\", 70)\n    if \"hr\" in blob_lower or \"personalo\" in blob_lower:\n        return (\"generic_hr\", 60)\n    return (\"unknown\", 0)\n```\n\nThen use this in your tier rules:\n\n```yaml\ntiers:\n  tier_1:\n    min_primary_score: 15\n    min_margin_over_second: 4.0\n    require_recruiter_gate: true\n    require_clear_winner: true\n    # NEW: persona authority gates\n    min_persona_authority: 80  # Only hiring_manager (85+) or internal HR (90+) or exec search (95+)\n    company_signals_any:\n      - \"michael kors\"\n      - \"michael page\"\n      - \"experis\"\n```\n\n---\n\n## Keyword Weighting Analysis: Current CV Fit Scoring\n\n### `matching_lib.score_variant()` Breakdown\n\nEach keyword hit **adds** to the primary score (capped at 3 hits):\n\n```python\ndef _keyword_score(keyword: str, jd_lower: str, title_boost_lower: str) -> tuple[float, int]:\n    count = jd_lower.count(kw)  # Raw count\n    capped = min(count, 3)       # Cap at 3\n    score = float(capped)        # Base: 1.0, 2.0, or 3.0\n    if kw in title_boost_lower:  # Title bonus\n        score += min(3.0, 2.0 + 0.5 * capped)  # +2.0 to +3.0\n    return score, capped\n```\n\n**Example scoring for luxury-retail variant:**\n\n```\nProfile: \"Area Manager, Premium Retail @ Michael Kors\"\nHeadline: \"Area Manager, Premium Retail\"\nAbout: \"10+ years luxury fashion... managing multi-site retail operations...\"\n\nKeyword matches:\n- \"area manager\" (headline): 1.0 base + 3.0 title bonus = 4.0\n- \"premium retail\" (headline): 1.0 base + 2.5 title bonus = 3.5\n- \"luxury\": 1.0 base + (not in headline, no title bonus) = 1.0\n- \"retail\": 1 hit + 1 hit (capped=2, use 2) + 2.0 title bonus = 4.0\n- \"fashion\": 1.0 base + 0 title = 1.0\n\nTotal primary_score = 4.0 + 3.5 + 1.0 + 4.0 + 1.0 = 13.5\n```\n\n### Firmographic Keyword Gaps\n\n**Currently missing from `variant_profiles.yaml` keywords:**\n\n1. **Staffing/recruitment specialist signals:**\n   - `\"michael page\"` (not a keyword, only checked in tier config)\n   - `\"heidrick & struggles\"`, `\"korn ferry\"`, `\"experis\"` (not in any variant keywords)\n   - `\"recruitment specialist\"`, `\"recruitment consultant\"` (no weighting)\n\n2. **Executive authority signals:**\n   - `\"executive search\"` (not in keywords)\n   - `\"retained search\"` (not in keywords)\n   - `\"chief people officer\"`, `\"vp people\"` (not in keywords)\n\n3. **Hiring decision signals:**\n   - `\"hiring authority\"` (concept, not token)\n   - `\"internal recruiter\"`, `\"in-house recruiter\"` (not in keywords)\n   - `\"hiring power\"` (not in keywords)\n\n**Fix:** Add these to **all variants** with **low weight** (so they don't break non-recruiter matches):\n\n```yaml\nluxury-retail:\n  keywords:\n    # ... existing keywords ...\n    # NEW: Recruiter/staffing signals (low weight, ~1pt each)\n    - \"recruiter\"\n    - \"recruitment\"\n    - \"talent acquisition\"\n    - \"executive search\"\n    - \"retained search\"\n    - \"michael page\"\n    - \"korn ferry\"\n    - \"experis\"\n    - \"headhunter\"\n    - \"in-house recruiter\"\n    # NEW: Hiring authority signals (low weight)\n    - \"hiring manager\"\n    - \"head of people\"\n    - \"chief people officer\"\n    - \"vp people\"\n```\n\n---\n\n## Proposed Scoring Formula Revision\n\n### Current (Simple Additive)\n\n```\nunified_score = max(\n    sector_top × 2.2 + cv_primary × 0.4,\n    cv_primary\n)\n```\n\n### Proposed (Firmographic Boost)\n\n```\npersona_authority_weight = detect_recruiter_persona(blob).weight  # 60–95\nfirmographic_boost = persona_authority_weight / 100.0             # 0.6–0.95\n\nunified_score = (\n    cv_primary × 0.5 +\n    sector_top × 1.8 +\n    firmographic_boost × 3.0  # Up to +2.85 points\n) × (\n    1.0 + (0.2 if language_match else 0.0)  # +20% if LT profile → LT variant\n)\n```\n\n**Example:** Executive search consultant with sector_top=6, cv_primary=11, language=LT:\n```\nOld: max(6×2.2 + 11×0.4, 11) = max(17.6, 11) = 17.6\nNew: (11×0.5 + 6×1.8 + 0.95×3.0) × 1.2\n   = (5.5 + 10.8 + 2.85) × 1.2\n   = 19.15 × 1.2\n   = 22.98 ← Substantial boost for authority + language alignment\n```\n\n---\n\n## Recommended Action Items\n\n### Immediate (Tier 1 — This Week)\n\n1. **Add staffing agencies to sector keywords** (5 mins)\n   - Add `\"michael page\"`, `\"executive search\"`, `\"retained search\"` to sector_keywords\n   - Test: Run dry-run on recent Michael Page profiles → should see score bump\n\n2. **Add persona detection function** (30 mins)\n   - Copy function above into `recruiter_match.py`\n   - Add to `match_recruiter_profile()` output as `persona_slug` + `persona_authority`\n   - Log the detected persona for dry-run review\n\n### Medium (Tier 2 — Next Review Cycle)\n\n3. **Implement language-first variant ordering** (1 hour)\n   - Pre-detect profile language (LT vs EN)\n   - Bias tier scoring toward `luxury-retail-lt` when profile is LT\n   - Test: Lithuanian-language profiles should default to LT variant\n\n4. **Add persona authority tiers** (1.5 hours)\n   - Refactor `tiers:` YAML to include `min_persona_authority` rules\n   - Update `meets_tier_scorebar()` to check persona authority\n   - Test: Tier 1 should reject generic-HR profiles (authority < 70)\n\n### Long-term (Tier 3 — Next Month)\n\n5. **Refactor scoring formula with firmographic weighting** (2 hours)\n   - Replace simple additive with composite formula above\n   - Add language-match boost\n   - A/B test new formula vs old on last 50 profiles → measure response rate lift\n\n---\n\n## Quick Validation: Current Tier Boundary Gaps\n\n| Persona | Company | Current Tier | Recommended Tier | Issue |\n|---------|---------|--------------|------------------|-------|\n| Internal HR Director | Michael Kors | Tier 1 ✓ | Tier 1 ✓ | Correct |\n| Retained Search | Korn Ferry | Tier 2 | **Tier 1** | Authority=95, should be higher priority |\n| In-house Recruiter | Michael Page | Tier 1 ✓ | Tier 1 ✓ | Correct (already in tier_1 company_signals) |\n| Generalist Recruiter | Random Staffing | Tier 3 | **Tier 4** | Authority=60, low hiring power → backlog |\n| Area Manager | Random Retail | Tier 2 | **Tier 2** | Correct (hiring authority + sector) |\n| VP People | Startup | Tier 2 | **Tier 1** | Authority=90, C-level → should rank higher |\n\n---\n\n## Summary: Firmographic Weighting Road Map\n\n✅ **Gate terms exist and work** — You correctly identify people in the hiring ecosystem  \n❌ **No authority hierarchy** — All gate terms are equal (recruiter = HR admin)  \n❌ **Staffing agencies underweighted** — Michael Page gets 0 sector points despite specializing in retail  \n❌ **Language signals arrive too late** — LT market profiles analyzed as English-first  \n⚠️ **Missing persona weighting in tier rules** — Can't prioritize by authority level\n\n**Fix priority:** Add persona detection → language-first matching → authority-gated tiers → revised scoring formula.\n"
