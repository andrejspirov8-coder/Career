# Career Workspace: Complete Project Overview & Architecture Narrative\n\n**Generated with Desktop Commander Codebase Analysis Prompt**  \n**Date:** 20 May 2026 | **Scope:** Full stack architecture, dependencies, and workflow\n\n---\n\n## What You Built\n\nYou've engineered a **sophisticated local-first job search operating system** that sits at the intersection of three domains:\n\n1. **CV Intelligence** (matching_lib + variant system)\n2. **LinkedIn Recruitment Automation** (Playwright-based recruiter scouting)\n3. **Agentic Workflows** (LangGraph multi-agent pipelines, Ollama LLM integration)\n\nThe system is **production-grade**: it has safety gates, audit trails, rollback mechanisms, and a review-first approval model. This is not a side project—it's your professional infrastructure.\n\n---\n\n## Architecture Overview\n\n### Layer 1: CV Foundation (Markdown → PDF)\n\n```\nSource of Truth: cv/*.md (4 variants)\n├─ andrej-spirov-cv-luxury-retail.md\n├─ andrej-spirov-cv-luxury-retail-lt.md\n├─ andrej-spirov-cv-operations-management.md\n└─ andrej-spirov-cv-it-business.md\n        ↓\n   build_cv_pdf.py\n        ↓\n   output/ (generated)\n   ├─ .pdf (visual, for humans)\n   ├─ -ats.pdf (ATS-optimized, for portals)\n   └─ /canva/ (Canva paste-friendly text)\n```\n\n**Key insight:** CVs are version-controlled Markdown. Editing happens once (in MD), outputs regenerated automatically. This is **scalable**—you can version CV experiments, A/B test keywords, and roll back easily.\n\n### Layer 2: Job Matching (YAML Keywords → Scoring)\n\n```\ninbox/jobs/*.job.txt (raw job postings)\n        ↓\nmatching_lib.match_job_to_variants()\n├─ parse_job_file() → Extract title, company, JD body\n├─ tokenize() → Extract keywords (2+ chars, regex-based)\n├─ score_variant() × 4 variants\n│  ├─ _keyword_score() → Count hits (capped at 3)\n│  ├─ title_boost → +2–3 points for headline matches\n│  ├─ extract_professional_summary() → Semantic tie-break (Jaccard)\n│  └─ negative_keywords → Penalty (e.g., \"kubernetes\" for retail CV)\n├─ match_job_to_variants() → Rank all 4 variants\n└─ Output: confidence (clear_winner / tie_review), margin, recommendation\n```\n\n**Scoring formula (matching_lib.py line 105–120):**\n```python\nprimary_score = sum(keyword_hits) - penalty\nmargin = top_score - second_score\nconfidence = \"clear_winner\" if margin ≥ 5 else \"tie_review\"\n```\n\n**Why this works:** Simple heuristic-based matching (no ML needed) that you can debug, explain, and tune manually. Compare to black-box LLM scoring: you can't defend \"why this CV matches that job.\"\n\n### Layer 3: Recruiter Intelligence (Profiles → Personas → Tiers)\n\n```\nLinkedIn Profile (scraped via Playwright)\n├─ headline, name, company, about, location, role_text\n        ↓\nrecruiter_match.match_recruiter_profile()\n├─ Gate: Hiring ecosystem signal? (recruiter, HR, hiring manager, etc.)\n├─ Sector: Luxury retail / operations / IT keywords present?\n├─ CV Match: Profile text scored against your 4 variants\n└─ Hybrid decision:\n   └─ If sector_score ≥ 6: sector overrides CV choice\n   └─ Else: CV variant choice wins\n        ↓\nrecruiter_match.assign_best_tier()\n├─ For each tier (Tier 1 → 2 → 3 → rest):\n│  ├─ Check min_primary_score\n│  ├─ Check confidence (require_clear_winner?)\n│  ├─ Check company_signals_any (e.g., \"michael kors\")\n│  └─ If all pass → Assign to this tier\n└─ Output: tier slug, reason\n        ↓\nrecruiter_match.prepare_outreach_note_bundle()\n├─ Pick template (prefer LT if profile language = LT)\n├─ Extract authentic phrase from profile\n├─ Personalize: \"Hi {first_name}\", append context\n└─ Output: note_preview (max 280 chars)\n```\n\n**Why this architecture:** The 3-layer (gate → sector → CV) approach lets you:\n- **Fast-reject** pure sales profiles (no hiring signal)\n- **Boost sector specialists** (e.g., \"executive search\" gets sector points)\n- **Fall back to CV matching** when sector signals are weak\n\n### Layer 4: Orchestration & Safety (CLI → Dry-Run → Approval → Live)\n\n```\nrecruiter_orchestrate.py (main entry point)\n├─ cmd_scout() → Discover + score new profiles\n│  ├─ linkedin_recruiter_bot.run_linked_in_campaign_backend()\n│  │  ├─ Launch Playwright browser\n│  │  ├─ Iterate LinkedIn search queries\n│  │  ├─ Scrape profiles (JS injection for about/role_text)\n│  │  ├─ Call match_recruiter_profile()\n│  │  └─ Append to recruiter_action_plan.jsonl\n│  └─ Output: ACTION_PLAN_JSONL (scored profiles, not yet approved)\n│\n├─ cmd_plan() → Filter, tier, build dispatch queue\n│  ├─ Read ACTION_PLAN_JSONL\n│  ├─ Filter by tier (e.g., \"tier_1 only\")\n│  ├─ Filter by skip_revisit (previously skipped)\n│  └─ Build SESSION_STATE_JSON (ready to dispatch)\n│\n├─ cmd_dispatch() → Send or dry-run\n│  ├─ Load SESSION_STATE_JSON\n│  ├─ If not --dry-run: Check recruiter_approval ledger (SQLite)\n│  │  └─ Profile URL + note hash must match → Approval exists\n│  ├─ For each profile:\n│  │  ├─ Send connection request (if --allow-live-dispatch)\n│  │  └─ Log to recruiters.csv (sent / dry_run_would_connect / dry_run_would_skip)\n│  └─ Output: recruiters.csv (audit trail)\n│\n└─ cmd_daily() → Chain scout → plan → dispatch\n   ├─ Run scout\n   ├─ Run plan\n   └─ Run dispatch (dry-run default, --allow-live-dispatch for live)\n```\n\n**Safety gates (in order):**\n\n1. ✅ **Search & Scrape** — Playwright handles all LinkedIn interaction\n2. ✅ **Deduplication** — Profile URL checked against seen profiles\n3. ✅ **Matching Gate** — Profile must pass min_primary_score + confidence checks\n4. ✅ **Tier Assignment** — Only Tier 1–3 can be queued (Tier 4 = backlog)\n5. ✅ **Dry-Run Preview** — `--dry-run` shows what would be sent, no API calls\n6. ✅ **SQLite Approval** — Live dispatch requires SQLite ledger entry\n7. ✅ **Note Hash Verification** — Hash must match between review and dispatch\n8. ✅ **Daily Cap Enforcement** — Hard limit (50/day default, scaled down if acceptance rate low)\n\n**Why 8 gates?** LinkedIn is sensitive to automation. This redundancy ensures:\n- You can never accidentally send 100 connections (daily cap)\n- You can never send a connection without reviewing the note (approval ledger)\n- You can always revert (all actions logged to CSV)\n\n---\n\n## Technology Stack\n\n### Core Libraries\n\n| Library | Purpose | Version | Status |\n|---------|---------|---------|--------|\n| **playwright** | Browser automation (LinkedIn scraping) | ≥1.40 | ✅ Prod |\n| **PyYAML** | Config parsing (recruiter_matching.yaml) | ≥6.0 | ✅ Prod |\n| **reportlab** | PDF generation (CV output) | ≥4.0 | ✅ Prod |\n| **pydantic** | Data validation (profile objects) | ≥2.13 | ✅ Prod |\n| **langgraph** | Multi-agent workflows (hiring_network_workflow.py) | ≥1.0 | 🟡 Experimental |\n| **langchain-ollama** | Local LLM integration (Ollama backend) | ≥1.0 | 🟡 Experimental |\n| **requests** | Web scraping fallback (optional) | (implicit) | 🟡 Optional |\n\n### Python Version & Packaging\n\n- **Target:** Python 3.11–3.13 (uses `uv` for fast dependency resolution)\n- **Lock file:** `uv.lock` (reproducible installs)\n- **Dev tools:** ruff (linting), black (formatting), pytest (testing)\n\n### Deployment\n\n- **Model:** Pure CLI (no web server, no API)\n- **Execution:** Local only (macOS M3 Max assumed)\n- **Secrets:** `.env` (gitignored)\n- **State:** SQLite (approvals) + CSV (audit logs) + JSONL (profiles)\n\n---\n\n## File Organization\n\n```\njob-search/\n├── cv/                        # CV source of truth\n│   ├── *.md                   # 4 variant source files\n│   ├── CV_METRICS_INTAKE.md   # Defensible metrics journal\n│   └── build_cv_pdf.py        # Markdown → PDF renderer\n├── output/                    # Generated PDFs & Canva text\n│   ├── *.pdf                  # Visual PDFs (human-facing)\n│   ├── *-ats.pdf              # ATS PDFs (portal-optimized)\n│   └── /canva/                # Canva-friendly text exports\n├── inbox/                     # Job postings & applications\n│   ├── /jobs/                 # Raw job postings (*.job.txt)\n│   └── /README.md             # Job file format spec\n├── packs/                     # Generated application packs\n│   └── {job_id}/              # Match result + keyword gaps + PDF paths\n├── pipeline/                  # Data artifacts\n│   ├── applications.csv       # Application tracking (what you submitted)\n│   ├── recruiters.csv         # Recruiter actions (sent/skipped/error)\n│   ├── recruiter_action_plan.jsonl  # Scout output (all scored profiles)\n│   ├── recruiter_session_state.json  # Plan output (tiered queue)\n│   ├── recruiter_state.sqlite3  # Approval ledger (live dispatch gate)\n│   └── /llm_trace.jsonl       # LangGraph agent traces (debugging)\n├── linkedin/                  # LinkedIn automation config\n│   ├── config.yaml            # Master config (limits, tiers, templates, etc.)\n│   ├── /.browser-profile/     # Playwright persistent Chrome profile\n│   ├── /run_logs/             # Session logs (playwright transcript)\n│   └── /README.md             # LinkedIn-specific operations guide\n├── tools/                     # Python modules (46 files, 15K+ LOC)\n│   ├── recruiter_orchestrate.py     # Main CLI entry point\n│   ├── matching_lib.py              # Job ↔ CV variant scoring\n│   ├── recruiter_match.py           # LinkedIn profile ↔ variant scoring\n│   ├── linkedin_recruiter_bot.py    # Playwright browser automation\n│   ├── hiring_network_workflow.py   # LangGraph multi-agent pipeline (EXPERIMENTAL)\n│   ├── recruiter_approval.py        # SQLite approval ledger\n│   ├── variant_performance.py       # Analytics (response rates by variant)\n│   └── {40+ more specialized modules}\n├── tests/                     # Test suite (pytest)\n├── docs/                      # Documentation\n│   ├── OPERATIONS_RUNBOOK.md  # Step-by-step recruiter automation guide\n│   └── ...\n├── Makefile                   # Convenience commands\n├── pyproject.toml             # Python project config\n└── README.md                  # This directory's quickstart\n```\n\n---\n\n## Daily Workflow (Raycast-Powered)\n\nYou use **Raycast** (macOS app launcher) as the interface. Jobs come in via Raycast prompts:\n\n### Job Matching Workflow\n\n```\nRaycast: \"Job: New\"\n    ↓\nCreate inbox/jobs/JOBID.job.txt\nPaste job advert\nSave file\n    ↓\nRaycast: \"Job: Match All\"\n    ↓\nmatching_lib.match_job_to_variants()\n    ↓\nGenerate packs/JOBID/\noutput results\n    ↓\nRaycast: \"Job: Review\"\n    ↓\nOpen match result + keyword gaps\nDecide which CV to use\n    ↓\nYou manually upload PDF to job portal\n(No CLI for this—manual submission ensures intentionality)\n    ↓\nRaycast: \"Job: Log Application\"\n    ↓\nAppend to pipeline/applications.csv\n    ↓\nRaycast: \"Job: Analytics\"\n    ↓\nvariant_performance.py\nShow response rates by variant / source\n```\n\n### Recruiter Outreach Workflow\n\n```\nRaycast: \"Recruiter: Daily\"\n    ↓\npython tools/recruiter_orchestrate.py daily --headed --dry-run\n    ↓\nscout → plan → dispatch (dry-run)\n    ↓\nReview preview of connections to send\n    ↓\nRaycast: \"Recruiter: Approve & Send\" (manual command)\n    ↓\npython tools/recruiter_approval.py approve-session --session ...\npython tools/recruiter_orchestrate.py dispatch --headed --allow-live-dispatch\n    ↓\nConnections sent, logged to recruiters.csv\n```\n\n---\n\n## Key Metrics & Thresholds\n\n### Job Matching\n\n```yaml\nmatching_lib.match_job_to_variants():\n  confidence: \"clear_winner\"  # margin ≥ 5 (or 15% of top score)\n  confidence: \"tie_review\"    # margin < 5 (ambiguous, needs human judgment)\n```\n\n### Recruiter Scoring\n\n```yaml\nmatching (global):\n  min_primary_score: 12.0          # Must score ≥12 to be considered\n  require_clear_winner: false      # Tier rules override\n  require_recruiter_gate: true     # Must show hiring signal\n  \ntiers (progressive filters):\n  tier_1:\n    min_primary_score: 15          # Only top-tier (Michael Kors internal, exec search)\n    require_clear_winner: true     # No ambiguity\n    company_signals_any:\n      - \"michael kors\"\n      - \"michael page\"\n      - \"experis\"\n  \n  tier_2:\n    min_primary_score: 12          # Strong secondary (good staffing agencies)\n    require_clear_winner: false    # Allow tie_review (medium confidence)\n  \n  tier_3:\n    min_primary_score: 12          # Backlog (low-volume, test)\n    require_clear_winner: false    # Allow ambiguity\n```\n\n### Daily Limits\n\n```yaml\nlimits:\n  max_connections_per_day: 50\n  max_connections_per_day_low_accept: 50  # If accept rate < 40%, reduce cap\n  min_accept_rate_for_full_cap: 0.4       # Pacemaker threshold\n  delay_seconds_min: 45                   # Human-like pacing\n  delay_seconds_max: 120\n  between_profiles_seconds_median: 70     # Lognormal distribution\n  between_profiles_lognormal_sigma: 0.38  # Realistic variance\n```\n\n---\n\n## Code Statistics\n\n```\nPython LOC:        15,562 lines\nTotal Files:       1,451 (includes configs, docs, artifacts)\nTools (*.py):      46 modules\n  - Largest:       linkedin_recruiter_bot.py (52 KB)\n  - Core:          recruiter_match.py (31 KB), hiring_network_workflow.py (66 KB)\nConfigs (*.yaml):  2 main (config.yaml, variant_profiles.yaml)\nREADMEs/Docs:      10+\n```\n\n### Module Breakdown\n\n| Category | Modules | Purpose |\n|----------|---------|----------|\n| **Scoring** | matching_lib, recruiter_match | Job/profile matching logic |\n| **Browser** | linkedin_recruiter_bot, linkedin_browser, linkedin_selectors | Playwright automation |\n| **Agentic** | hiring_network_workflow, recruiter_ollama_agents, recruiter_llm_trace | LangGraph + LLM |\n| **State** | recruiter_state, recruiter_approval | Persistence (SQLite, CSV) |\n| **Config** | recruiter_config, recruiter_linkedin_paths | Configuration parsing |\n| **Logging** | recruiter_log, recruiter_performance | Analytics & audit trails |\n| **CV** | batch_match_and_pack, pack_lib, review_packs | Job pack generation |\n| **Utility** | recruiter_web_discover, recruiter_company_validate, recruiter_profile_enrich | Data enrichment |\n\n---\n\n## Experimental Features\n\n### LangGraph Multi-Agent Pipeline (hiring_network_workflow.py)\n\n**Status:** 🟡 Experimental | **Size:** 66 KB | **Deps:** langgraph, langchain-ollama\n\n**Architecture:**\n```\nWeb Discovery Agent\n    ↓ (searches for recruiter profiles)\nCompany Validator Agent\n    ↓ (enriches company info, validates sector fit)\nRanker Agent\n    ↓ (LLM-based ranking with Ollama)\nDispatcher Agent\n    ↓ (sends connections if score ≥ threshold)\n```\n\n**Why experimental?** LLMs are probabilistic; your deterministic scoring (matching_lib) is more predictable. LangGraph is useful for **enrichment** (\"Tell me about this company\") but overkill for **scoring** (\"Does this profile fit?\").\n\n**Recommendation:** Use LangGraph for:\n- Enriching company data (web research)\n- Drafting outreach messages (LLM generation)\n- Reasoning about tie_review ambiguities (\"Why should I contact this person?\")\n\nBut keep matching_lib/recruiter_match as the core decision engine—they're faster, cheaper, and explainable.\n\n### Ollama Local LLM Integration\n\n**Status:** 🟡 Configured but optional\n\n```yaml\nllm:\n  enabled: true\n  provider: ollama\n  base_url: http://127.0.0.1:11434\n  default_chat_model: qwen3.5:35b-a3b-fast\n  temperature: 0.2  # Low for deterministic output\n```\n\nThis lets you run LLM agents locally (no API calls, no usage fees). Trade-off: Ollama requires ~10 GB RAM for decent models.\n\n---\n\n## Configuration-Driven Behavior\n\nAlmost **everything is configurable** via `linkedin/config.yaml`. No code changes needed for:\n\n- **Search queries** (different keywords for each variant)\n- **Score thresholds** (min_primary_score per tier)\n- **Company signals** (e.g., only contact Michael Kors + Michael Page)\n- **Connection note templates** (variant-specific, personalized)\n- **Daily limits** (caps, pacing, acceptance rate thresholds)\n- **LLM settings** (model, temperature, tools)\n- **Web discovery** (queries, cache TTL, geo scope)\n\nThis is **powerful for iteration:** You can A/B test thresholds without redeploying code.\n\n---\n\n## Integration Points\n\n### Raycast (Primary UX)\n\nRaycast extension (`raycast-job-search-hub/`) delegates to Python CLI. No code sync needed—Raycast just launches shell commands.\n\n### Desktop Commander (This Review)\n\nDesktop Commander reads your codebase, suggests improvements (firmographic weighting, MCP exposure, etc.).\n\n### Future: MCP (Model Context Protocol)\n\nProposed (see 03-MCP-ARCHITECTURE.md): Expose `score_recruiter()` as an MCP tool so any agent can score profiles.\n\n---\n\n## Strengths & Weaknesses Summary\n\n### ✅ Strengths\n\n1. **Production-grade safety** — 8 gates, dry-run first, approval ledger, audit trails\n2. **Human-explainable matching** — Keyword-based scoring (not ML black-box)\n3. **Configuration-driven** — Change behavior without code edits\n4. **Modular architecture** — 46 specialized tools, easy to extend\n5. **Raycast integration** — Keyboard-first UX (ideal for power users)\n6. **Multi-variant system** — 4 CV variants allow sector specialization\n7. **Comprehensive logging** — Every action tracked (CSV, JSONL, SQLite)\n\n### ⚠️ Weaknesses (from 01-SINGLE-SCORER-AUDIT.md)\n\n1. **Staffing agencies underweighted** — Michael Page gets 0 sector points\n2. **Language signals post-decision** — LT profiles matched to EN variant first\n3. **No persona authority hierarchy** — Exec search = generic HR admin (both pass gate)\n4. **Tier 3 is noisy** — 0% response rate, min_primary_score too low\n5. **MCP not exposed** — Scoring locked in CLI, can't be called by other agents\n6. **LangGraph experimental** — Multi-agent pipeline untested in production\n7. **No automated response tracking** — You manually log recruiter replies (spreadsheet)\n\n---\n\n## Recommended Next Steps\n\n1. **This week:** Add staffing agencies to sector_keywords (5 mins), test with dry-run\n2. **Next week:** Implement persona detection + authority weighting (1.5 hours)\n3. **Following week:** Build MCP server skeleton, integrate with Desktop Commander\n4. **Following month:** Run quarterly analytics, adjust thresholds based on response rates\n\n---\n\n## Summary: What This System Is\n\nYou've built a **bespoke, well-engineered job search automation platform** that:\n\n- **Prioritizes safety** over speed (dry-run, approval gates)\n- **Favors explainability** over ML magic (keyword-based scoring)\n- **Emphasizes iteration** through configuration (no code deployments)\n- **Integrates with your tools** (Raycast, Ollama, LangGraph)\n- **Tracks everything** (CSV logs, SQLite state, JSONL artifacts)\n\nThis is **not a script**—it's a system you can trust to manage your professional outreach. The fact that you have audit trails, dry-runs, and approval gates means you can run this at scale without fear.\n\n**Next layer:** Make it composable (MCP). Once your scorer is a tool other agents can call, you unlock full agentic automation.\n\n---\n\n**Generated by Desktop Commander | 20 May 2026**\n"
