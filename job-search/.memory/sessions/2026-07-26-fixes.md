# 2026-07-26 — Fix session (post-audit)

## What was done

Fully resolved the 17 Python test failures and cleaned up lint + npm:

### P0: Python test failures (17 → 0)
- **2 import errors**: `canonical_linkedin_job_url` and `job_id_from_url` were removed from `opportunities.browser` and `integrations.linkedin.opportunities` during URL refactoring. Fixed tests to import from `core.linkedin_urls` instead.
- **15 functional regressions**: Root cause was `SCHEMA_SQL` being a comment-only string — `migrate_schema()` runs `INSERT INTO schema_meta` after executing it, but no `CREATE TABLE IF NOT EXISTS schema_meta` existed. Added the CREATE to `SCHEMA_SQL`.

### P1: Architecture test
- Added `.memory` to expected top-level set.

### P2: npm vulnerabilities
- Dashboard: `next` 16.2.9→16.2.11, `postcss` override 8.5.10→8.5.18, `json-schema-to-typescript` 15.0.0→15.0.4. Vulnerabilities: 10→2.
- Raycast: no safe fixes possible (all through `@raycast/api` → `@oclif`).

### P3: Lint cleanup
- S105: 3 false positives in `mcp/server.py` (env var names, not secrets) → `# noqa: S105`
- B011: `assert False` → `raise AssertionError(...)` in 2 test files
- I001: auto-fixed 3 import-sort issues

## Verification
- Python tests: ✅ all pass
- Architecture tests: ✅ 10/10
- Dashboard tests: ✅ 22/22 (76 tests)
- Raycast tests: ✅ 7/7 (29 tests)
- Ruff: 51 remaining (all accepted — E402 intentional, S603 subprocess, etc.)
