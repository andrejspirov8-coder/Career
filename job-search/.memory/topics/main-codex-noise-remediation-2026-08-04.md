- 2026-08-04 — Corrected + applied remediation from /tmp/full_codebase_review.md (flawed review validated against real codebase). Dispatch 4 subagents (A-D), all green. DONE: (A) variant_profiles.yaml - removed noisy target_titles Pamainos vadovas (luxury-retail-lt), Komandos/Padalinio vadovas (ops-mgmt-lt); added 9 industry negatives (gamybos/statybos/vairuotojas/kasininkas/mesininkas/virejas/sandelio/valytojas/pakuotojas) to 4 variants; removed Russian/English/Lithuanian (luxury-retail) + admin/administrator (it-business); (B) deleted dead scoring.py, repointed test_opportunities_matching.py imports to matching.py, +4 salary-guard tests (sub-100 floor in extract_salary_text); (C) deleted stale config/opportunities.yaml (gitignored, was user-only, inbox.enabled:false never affected automation - root cause was automation runs on opportunities.example.yaml); (D) wrote corrected review /tmp/full_codebase_review_corrected.md. Gate: re-match 1187 opps -> matched 114->84, review 974->1004, apply_ready still 6 (legit Bolt/SEB); noise: Pamainos vadovas 38-><=17.3, most <5, noisy_apply_ready 0. ruff pass, pytest 758 (754+4 new), dashboard-test 72+tsc, verify-release green EXCEPT pre-existing cryptography audit vuln. Also fixed stale ruff cmd in AGENTS.md: 'tools cv tests'->'src cv tests' (now-empty tools/ dir). NOT committed (per policy). FOR MAIN CODEX: (1) cryptography>=48.0.1,<49 pinned in pyproject (via pyjwt[crypto] Supabase auth) now flagged by pip-audit (fix 49/50) - bump+lockfile = Main Codex scope; (2) latent matching.py salary_score bug parses comma as decimal so European thousands '3,400-4,500' -> 1.0 no salary boost downstream (text.py now posts it correctly though); C (3) still-live leaked PAT [REDACTED] + Supabase HIBP password check (from prior handoff, still open). Dirty worktree preserved: variant_profiles.yaml, AGENTS.md, text.py, test_opportunities_matching.py, scoring.py deleted, dashboard/next-env.d.ts, supabase files.
- 2026-08-04 — ADDENDUM: Fixed latent salary_score bug in matching.py (_parse_salary_amounts): comma/dot now treated as thousands separator when grouping exactly 3 digits (3,400->3400) vs decimal (3.4); added ValueError guard for mixed separators (1,234.56). Updated +3 salary-guard tests (3,400-4,500 now ->1.9 not 1.0; added 3.400-4.500->1.9, 3,4->1.0). Bumped cryptography pin pyproject.toml>=48.0.1,<49 -> <51, uv lock --upgrade-package cryptography resolved 48.0.1->50.0.0 (all 3 advisories fixed); pip-audit now clean, direct deps import OK, pytest 760 green. Full gate green EXCEPT commit (uncommitted per policy). Remaining user-action security items (NOT code): revoke leaked PAT [REDACTED], enable Supabase HIBP leak check.
- 2026-08-04 — CORRECTION to earlier handoff: settings.yaml IS wired (recruiters/config.py typed loader + api/routers/settings_runtime.py GET/POST + recruiters/settings_helper.py CLI + test_runtime_settings_helper.py round-trip). BUT it is only a dashboard/CLI-managed store with NO consumer in the live dispatch path: orchestrator.py reads dry_run/max_dispatch/db_path from function args, policy.py, and repository.DEFAULT_STATE_DB (e.g. orchestrator.py:565). So runtime.settings fields (max_live_dispatch_batch, stop_on_captcha, require_live_dispatch_ack, state.database_path) have an editor but no dispatch-time consumer. This is a REAL integration gap (NOT the dead-config Phase-7 I earlier flagged) and is MAIN-CODEX scope: wiring it changes live-recruiter dispatch safety. Do not change unilaterally.
- 2026-08-04 — settings.yaml dispatch wiring: implemented CONSERVATIVE wiring only. cmd_dispatch now uses load_settings() to derive effective_dry_run = dry_run OR (not allow_live_dispatch AND (mode==dry_run OR dry_run_default)). This is provably safe: settings only force MORE dry-run and never disable the live gate (allow_live_dispatch, LINKEDIN_SEND_MODE=cli_gated, approval ledger, validate_live_dispatch_max all unchanged). DELIBERATELY NOT wired (safety regressions, found via review + tests): (1) limits.max_live_dispatch_batch as --max fallback would break test_live_dispatch_rejects_missing_or_oversized_max which requires explicit --max; (2) state.database_path for approval ledger - live settings.yaml has leftover test artifact state/test.sqlite3 (nonexistent), would redirect ledger; (3) runtime.require_live_dispatch_ack / require_approval_ledger / stop_on_* - not wired to avoid loosening CLI/env gates. Gate: ruff clean, recruiter tests 29 passed, full suite 760 passed, dashboard-test 72+tsc pass. Config note: config/settings.yaml (untracked) still carries test round-trip values (mode: dry_run, database_path: state/test.sqlite3, stop_on_checkpoint: false) - harmless under conservative wiring but should be sanitized to real values (database_path: state/recruiter_state.sqlite3). Committed: 69aece7 (feat: wire settings.yaml dry-run defaults into recruiter dispatch).
