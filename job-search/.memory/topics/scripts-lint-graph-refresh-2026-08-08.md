- 2026-08-08 — Lint scripts/ + graph refresh (quick wins). (1) Added "scripts/**/*.py" to [tool.ruff] include in pyproject.toml; fixed the 3 findings it exposed: coverage_ratchet.py S108 (predictable /tmp/career_coverage_ratchet.json -> tempfile.mkstemp with cleanup); generate-contracts.py S607 x2 ("uv"/"npx" bare-name subprocess -> _which() via shutil.which, fails loudly if missing). ruff check + ruff format --check clean (266 files), make contracts-drift green (generated TS unchanged), make coverage-ratchet green (59.69% >= 59.6%). Committed bfe215f (3 files). Auto-generated dashboard/next-env.d.ts (Next.js routes types import) was swept into the follow-up memory commit - committed, tree clean. (2) Re-indexed codebase-memory graph (full) - verified stale supabase code nodes gone (only archived SQL + memory docs + test_api_auth guard tests remain) and opportunities/sources/ registry nodes present. FOR MAIN CODEX/USER: still-open items unchanged - revoke leaked GitHub PAT [REDACTED] (gh uses it; revoke at github.com/settings/tokens then 'gh auth login'), then push 19 unpushed commits; 3 unfixable npm advisory chains (undici/js-yaml/postcss) when patches land; optional later: pyright expansion to cvs/recruiters/automation/workspace, frontend ESLint setup, dead SUPABASE_* env vars in shell profile.
