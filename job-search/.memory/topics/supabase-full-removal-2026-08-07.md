- 2026-08-07 — 2026-08-07: REMOVED SUPABASE ENTIRELY (user-approved). DISCOVERY: runtime auth was ALREADY fully local (HMAC bearer CAREER_DASHBOARD_TOKEN fail-closed + PBKDF2-600k SQLite users.sqlite3 + dashboard signed session cookie career_dashboard_session httpOnly/sameSite=strict/secure-in-prod); supabase>=2.31.0 was a DIRECT dep imported NOWHERE (src/scripts/dashboard), and the remote project pmyerkymcueaxerpmesk held only dormant mirror tables + Auth. Earlier .memory claims "Supabase = Auth only" were STALE (commit d55566e made auth local). CHANGED: (1) uv remove supabase - pruned 13 pkgs (postgrest, realtime, storage3, supabase-auth, supabase-functions, pyjwt, yarl, strenum, multidict, propcache, deprecation...); cryptography>=48.0.1,<51 STAYS (direct dep, workspace/backups.py AESGCM+Scrypt). (2) Deleted scripts/migrate_supabase_data.py + supabase/migrate_private.py; moved supabase/migrations/ (6 SQL) + README -> docs/archive/supabase-migrations/; removed supabase/ dir. (3) Deleted .env.local (held live SUPABASE_URL + publishable key + SERVICE_ROLE_KEY; gitignored, service-role JWT was verified NEVER in git history). (4) Auth hardening: api/user_store.py now has per-user failed-login lockout (in-memory, 5 fails/15min sliding window, thread-safe, generic 401 no enumeration, clears on success) + 4 new tests (lockout, per-user isolation, clear-on-success). Dashboard login cookie ALREADY had httpOnly/strict/secure(NODE_ENV=production) - no change. (5) Docs: ARCHITECTURE.md "No remote services" section; SCHEMAS.md supabase paragraph updated. GATE: test_user_store 6 passed, ruff clean, uv lock --check OK. FOR MAIN CODEX: DELETE remote project pmyerkymcueaxerpmesk (console or 'supabase projects delete'; MCP server needs SUPABASE_ACCESS_TOKEN, not configured this session) - clears rls_enabled_no_policy + auth_leaked_password_protection advisories; mirror data is a copy of local SQLite, nothing to back up. Also still-open: revoke leaked GitHub PAT [REDACTED] before push, 3 unfixable npm advisory chains (undici/js-yaml/postcss). NOTE: guard test dashboard/lib/server/fastapi-bridge.test.ts 'never reads a Supabase cookie' intentionally kept.
- 2026-08-07 (follow-up) — REMOTE PROJECT DELETED via Management API (SUPABASE_ACCESS_TOKEN env). Pre-delete inventory confirmed only the 6 dormant mirror tables (daily_runs, notification_settings, notifications, opportunities, opportunity_aliases, source_runs) + Auth; org had exactly 1 project (pmyerkymcueaxerpmesk, eu-west-1). DELETE /v1/projects/pmyerkymcueaxerpmesk -> HTTP 200; verified: projects list count 0, direct GET returns "Resource has been removed". rls_enabled_no_policy + auth_leaked_password_protection advisories now moot (project gone). NOTE: gh CLI was authenticated with the leaked PAT (prefix match [REDACTED]) - since revoked 2026-08-08 and gh re-authenticated with a new token (gho_5s1c...); GitHub has NO API to revoke a classic PAT by value, must use https://github.com/settings/tokens. Stale SUPABASE_PROJECT_REF/SUPABASE_ACCESS_TOKEN/SUPABASE_SERVICE_ROLE_KEY env vars remain in shell profile (dead now) - user may remove.
