- 2026-08-02 — 2026-08-02: Retired the legacy `tools/` shim layer end-to-end (TDEBT-011), commit `7a2359a`. Deleted all 72 `tools/*.py` sys.modules aliasing shims; re-pointed every consumer to `python -m career_job_search.X` / full package imports: 47 test files (dropped TOOLS_DIR/sys.path hacks), Makefile CLI targets, scripts (preflight.sh, smoke_repo.sh, verify_patch.sh), api/routers/helpers.py registry, dashboard python-bridge.ts (spawn now `uv run python -m ...`), scripts/generate-contracts.py (HELPERS → module paths, `-m`), workspace dashboard_service worker spawn + control.py launchd plist (now `-m career_job_search.workspace.dashboard_service --mode production`), dashboard repo-data/recruiter-components/e2e fixtures, integration conftest (HELPER_MODULES map + `-m` dashboard spawn). Added shared `src/career_job_search/core/entrypoint.py` (run/entry helpers for `--schema`+main) and `src/career_job_search/cvs/catalogue_cli.py` (real replacement for tools/cv_catalogue.py); retrofitted `__main__` blocks of 9 helper modules. Fixed 2 dynamic `import_module("recruiter_graph_workflow")` in recruiters/hiring_network.py (:300/:765) → `career_job_search.recruiters.graph_workflow`. pyproject: pythonpath `["src","cv"]`, ruff src `["src","cv","tests"]`. Updated docs (ARCHITECTURE, OPERATIONS_RUNBOOK, VILNIUS, README, SECURITY, linkedin/README), prompts (RECRUITER_AGENT_ORCHESTRATION, RECRUITER_AGENT_PROMPT), .cursor skill, AUDIT register (TDEBT-010 + TDEBT-011 CLOSED, 12/14, ~92/100). Contracts regenerated byte-identical; dashboard/lib/generated/* NOT changed. Left `dashboard/next-env.d.ts` unstaged (auto-generated artifact). Full gate green: ruff, 748 pytest (incl. integration), dashboard-test (72 vitest + typecheck), test-coverage (58%), make verify-release. IMPORTANT for Main Codex: any installed launchd plist for the dashboard still references `tools/dashboard_service.py`; regenerate/reinstall it to pick up the `-m` command (source: workspace/control.py `_launch_agent_payload`). Historical refs left untouched: archive/, .memory/topics/.
