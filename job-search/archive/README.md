# Historical archive

This directory preserves earlier plans, implementation prototypes, execution
reports, and dependency snapshots. Nothing here is an active command,
dependency source, runtime module, or setup instruction.

Use the root `README.md`, `docs/OPERATIONS_RUNBOOK.md`, and
`docs/ARCHITECTURE.md` for current behavior. Archived paths and command examples
are intentionally not maintained.
