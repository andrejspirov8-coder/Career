# The dashboard supervisor creates a private value automatically. Use this
# template only for a manual setup, and never commit the resulting .env.local.
CAREER_DASHBOARD_TOKEN=replace-with-a-long-random-local-token
